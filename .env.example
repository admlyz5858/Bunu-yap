# Optional: OpenAI API key for AI task splitting
# Without this, the app uses an offline fallback
VITE_OPENAI_API_KEY=sk-...

# Optional: Unsplash access key for background images
# Without this, gradient backgrounds are used
VITE_UNSPLASH_ACCESS_KEY=...
